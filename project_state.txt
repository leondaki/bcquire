PROJECT STATE — Acquire Clone (Godot)
Last updated: 2026-06-27

================================================================================
1. MAIN GOALS
================================================================================
- Faithful clone of the board game Acquire: identical rules/mechanics to the
  original, verified against the official rules text (ACQUIRE RULES.txt, root).
- Playable online multiplayer, eventually.
- Re-themeable: art assets, hotel-chain names/colors, and labels must be
  swappable later (different theme/IP) WITHOUT touching any rules/game logic.
- Locally testable in two senses, both already working:
    a) rules/mechanics — headless, no UI, no network required.
    b) full interactive game loop — playable hotseat in one Godot window.
- Eventually packaged for Steam release (Windows + Linux/Steam Deck) under the
  new theme.

================================================================================
2. OVERALL ARCHITECTURE
================================================================================
Engine: Godot 4.7 (GDScript only, GL Compatibility renderer).
Godot console exe (Windows, for headless/test runs):
  C:\Users\leond\AppData\Local\Microsoft\WinGet\Packages\
  GodotEngine.GodotEngine_Microsoft.Winget.Source_8wekyb3d8bbwe\
  Godot_v4.7-stable_win64_console.exe
Project root: C:\Users\leond\OneDrive\Desktop\bcquire (git repository,
initialized mid-project — see section 4's PROJECT-LEVEL CONSTRAINTS).

CORE DESIGN PATTERN: strict separation of rules engine / theme / UI / (future)
networking. This is the single load-bearing decision in the codebase:
  - core/        Pure rules engine. RefCounted classes only. No Node, no UI,
                 no theme, no networking. Fully deterministic. Drives itself
                 via explicit method calls (do_found, do_grow, merger_begin,
                 buy_stock, etc.) — these are exactly the calls net/session.gd
                 now drives on the host's behalf (M4 Stage A, see section 3).
  - net/         M4 multiplayer plumbing (Stages A+B done, see section 3):
                 action.gd/event.gd (Dictionary schemas), network_transport.gd
                 (abstract interface, RefCounted), loopback_transport.gd +
                 loopback_hub.gd (zero-socket, headlessly-testable transport),
                 enet_transport.gd (real ENetMultiplayerPeer LAN transport —
                 extends Node, not NetworkTransport, since Godot's high-level
                 RPC system requires a Node; duck-typed to the same surface),
                 session.gd (GameSession — host-authoritative router between
                 GameState and whichever transport is active), net_config.gd
                 (NetConfig autoload — carries a menu choice across the
                 MainMenu.tscn -> Game.tscn scene swap, see ui/menu/ below).
                 core/ still never imports net/ — the dependency runs
                 net/ -> core/, one direction, same as ui/ -> core/.
  - ui/menu/     Pre-game menu (project.godot's run/main_scene as of the
                 "presentation polish" plan's first stage, see section 3):
                 MainMenu.tscn + menu.gd. Collects a player name and a mode
                 (Hotseat/Host/Join), stashes it in the NetConfig autoload,
                 then change_scene_to_file()'s to ui/game/Game.tscn, whose
                 _ready() reads NetConfig and auto-starts the matching
                 session.
  - theme/       ThemeDef resource (theme_def.gd) + default_theme.tres. ALL
                 cosmetic config: board size/tile size, palette, per-chain
                 display name + color (indexed by AcqEnums.ChainId). UI reads
                 ONLY from ThemeDef; core/ never imports theme/. Re-skinning =
                 new .tres + art, zero logic changes.
  - ui/game/     The interactive game (current main scene). Controller
                 (game.gd) owns a GameSession (net/session.gd) and reads its
                 GameState mirror; drives the turn through phases by sending
                 Actions and reacting to session.event_applied — builds the
                 entire UI procedurally in code (no scene sub-nodes beyond the
                 bare Control root) for steppers, popups, cards, etc. Defaults
                 to a solo hotseat_mode session, so today's local play is
                 unchanged even though every mutation now goes through net/.
  - ui/board/    LEGACY M1 sandbox (Tile.tscn/board.gd). Superseded by
                 ui/game/. Still present on disk but not the run scene and
                 not maintained — safe to delete later, harmless to ignore now.
  - sim/         Headless test suites (SceneTree scripts, no GUT dependency).
                 Includes run_network_tests.gd (M4 Stage A): host/client
                 GameState convergence test over a loopback transport.

FOLDER STRUCTURE (actual files on disk):
  project.godot                  main_scene = res://ui/menu/MainMenu.tscn
                                  (was ui/game/Game.tscn before the main-menu
                                  stage landed — see section 3). [autoload]
                                  registers NetConfig = net/net_config.gd.
                                  stretch mode = disabled (1:1 pixel viewport,
                                  no canvas_items scaling), window resizable,
                                  min_size enforced at runtime (1000x620).
  ACQUIRE RULES.txt              official rules reference text (root dir).
  project_state.txt              this file.

  core/enums.gd                  AcqEnums: board geometry (12x9=108 tiles),
                                  ChainId enum (7 chains, tier-grouped:
                                  cheap=TOWER,LUXOR; medium=AMERICAN,
                                  WORLDWIDE,FESTIVAL; expensive=IMPERIAL,
                                  CONTINENTAL), GamePhase enum, PlacementKind
                                  enum (ISOLATED/FOUND/GROW/MERGE/
                                  ILLEGAL_DEAD/ILLEGAL_TEMP), tile_label()
                                  coordinate helper ("7C" style).
  core/stock_market.gd            StockMarket: pure price(chain,size) and
                                  majority_bonus()/minority_bonus() (10x/5x)
                                  functions. Verified against the rules doc's
                                  own worked example (Imperial size 5 -> 700,
                                  7000, 3500 — exact match).
  core/game_state.gd               GameState: the whole rules engine.
                                  - board: PackedInt32Array, CELL_EMPTY /
                                    ChainId.NONE (lone tile) / chain id.
                                  - classify_placement() decides
                                    found/grow/merge/dead/temp WITHOUT
                                    mutating anything (UI previews legality
                                    from this before allowing a drag-drop).
                                  - merger_info()/merger_begin()/
                                    merger_dispose()/merger_finish(): merger
                                    survivor sizes are computed BEFORE the
                                    connecting tile is added (new tile never
                                    counts toward either chain's pre-merger
                                    size — verified by test).
                                  - _pay_bonuses(): majority/minority with
                                    sole-holder-gets-both and tie-splitting
                                    (rounded up to nearest $100), matches
                                    rules doc exactly.
                                  - setup_new_game(): full deal INCLUDING the
                                    rules-mandated starting-draw phase (see
                                    section 3/4).
                                  - setup_blank(): no tiles dealt, for tests
                                    that hand-build exact board positions.
                                  - compute_final_scores(): end-game payout +
                                    stock liquidation.
  sim/run_tests.gd                 Headless rules test suite. 77 assertions,
                                  0 failures. Run via:
                                    godot --headless --path <proj> --import
                                    godot --headless --path <proj> --script sim/run_tests.gd
                                  Expect "==== N passed, 0 failed ====", exit 0.
  sim/check_midgame.gd              Headless UI-integration check: instances
                                  Game.tscn, calls _generate_midgame(), and
                                  asserts both engine state AND UI-side
                                  derived flags (board cell .playable flags)
                                  are correct. 18 assertions, 0 failures.
                                  Must run with --process deferral (loads
                                  scene + calls _ready before asserting) —
                                  see file header comment for why.
  sim/check_near_endgame.gd         Headless UI-integration check: instances
                                  Game.tscn, calls _generate_near_endgame(),
                                  and drives the real session/Action path
                                  (grow the one unsafe chain to safe, then
                                  declare the end) asserting can_end_game()
                                  flips false->true, the "Declare Game End"
                                  button appears/disappears correctly, and
                                  the GAME_OVER summary screen renders sorted
                                  final scores + a "New Game" button.
                                  20 assertions, 0 failures.
  sim/check_menu_flow.gd            Headless check for the main-menu/
                                  NetConfig plumbing: MainMenu.tscn's
                                  controls exist; a hotseat NetConfig intent
                                  is consumed/cleared by Game.tscn's _ready()
                                  and carries the chosen name to seat 0; a
                                  host NetConfig intent auto-hosts and is
                                  consumed/cleared; no pending intent falls
                                  back to legacy default-hotseat behavior;
                                  the game-over screen has both "New Game"
                                  and "Main Menu" buttons. References the
                                  NetConfig autoload via
                                  get_root().get_node("NetConfig") rather
                                  than the bare identifier, since a --script
                                  entry point's own top-level script
                                  compiles before autoloads are registered
                                  as global identifiers (game.gd, loaded
                                  later via load() at runtime, can reference
                                  NetConfig directly with no such issue).
                                  18 assertions, 0 failures.
  sim/check_animations.gd           Headless check for the animation layer,
                                  with animations left ON (the default — see
                                  game.gd's _animations_enabled). A _step
                                  state machine polled across real
                                  _process() ticks (Tweens need real frames/
                                  time): sends an animated TILE_PLACED action
                                  and asserts _animating flips true and
                                  drag/drop is locked out immediately, polls
                                  until the tile-slide finishes and asserts
                                  _animating clears with the model exactly
                                  where the no-animation path already proves
                                  correct, then repeats the same shape for a
                                  MERGER_STARTED merge-flash animation.
                                  10 assertions, 0 failures.
  sim/check_seat_gating.gd          Headless check for M4 Stage C's seat-
                                  gating: instances Game.tscn TWICE (ctrl_a =
                                  local seat 0, ctrl_b = local seat 1)
                                  sharing one hand-authored GameState object
                                  assigned directly to both controllers'
                                  state/session.state fields, with
                                  hotseat_mode = false on both sessions.
                                  Proves _rebuild_action_area()/
                                  can_drag_tiles() gate the right seat across
                                  PLACE_TILE/FOUND_CHAIN/BUY_STOCK (all three
                                  share one _is_my_turn() check) and that
                                  RESOLVE_MERGER's dispose sub-mode gates per-
                                  shareholder via _is_my_seat() on
                                  _disposal_queue[0].player, independent of
                                  state.current_player. Also pins
                                  can_drop_tile()'s current contract: no seat
                                  check of its own, relies entirely on
                                  can_drag_tiles() (via
                                  RackTile._get_drag_data()) having already
                                  refused to start the drag for an inactive
                                  seat. No real session.send_action() calls —
                                  every mutation is direct state/controller-
                                  field assignment + _refresh_all(), the same
                                  technique _generate_midgame()/
                                  _generate_near_endgame() already use.
                                  23 assertions, 0 failures.

  theme/theme_def.gd                ThemeDef resource script: board_width/
                                  height, tile_size, full color palette incl.
                                  color_playable (yellow legal-square hint),
                                  chain_names (PackedStringArray) and
                                  chain_colors (PackedColorArray), indexed by
                                  ChainId.
  theme/default_theme.tres          The only theme currently authored.

  ui/game/Game.tscn                 Main scene (just a bare Control + script).
  ui/game/game.gd                   Controller. Builds the ENTIRE UI in code
                                  in _build_layout(). Owns `state: GameState`
                                  and is the only place that calls into
                                  core/. Key structure (see section 4 for the
                                  sizing-bug history):
                                    main_vbox
                                      content_row (HBox, expand-fill)
                                        left_scroll -> left VBox: title,
                                          board grid, message label, bottom
                                          row (chains table | price chart)
                                        right_scroll -> side VBox: header,
                                          phase label, rack, stock cards,
                                          action box (phase-dependent)
                                      footer (HBox, pinned OUTSIDE any
                                        scroll — Generate Mid-Game /
                                        Generate Near-Endgame / Reset
                                        buttons, always visible)
                                  Phase state machine drives _action_box
                                  contents: PLACE_TILE / FOUND_CHAIN /
                                  RESOLVE_MERGER (tie sub-mode or dispose
                                  sub-mode) / BUY_STOCK / GAME_OVER.
                                  _generate_midgame() builds a hand-authored
                                  scenario covering every UI case at once
                                  (2 safe chains, all 7 chains on board, a
                                  dead tile, a temp-blocked tile, a tie
                                  merge, a normal merge, a grow, an isolated
                                  placement) — primary manual-test entry
                                  point, also driven by sim/check_midgame.gd.
                                  _generate_near_endgame() builds a separate
                                  scenario one grow away from can_end_game()
                                  (2 safe chains + 1 chain one tile short of
                                  safe) so the "Declare Game End" button and
                                  GAME_OVER summary screen can be exercised
                                  without playing a full game — also driven
                                  by sim/check_near_endgame.gd.
  ui/game/board_cell.gd              BoardCell (Button): one board square.
                                  Pure view of GameState.cell(); colors from
                                  ThemeDef. `playable` flag drives the yellow
                                  legal-square shading. Drag-drop TARGET:
                                  only accepts a drop whose tile coord
                                  exactly matches its own (col,row).
  ui/game/rack_tile.gd                RackTile (Button): one hand tile. Drag-
                                  drop SOURCE. `dead` flag -> styled red,
                                  un-draggable (engine still allows a manual
                                  Redraw button next to it in game.gd, this
                                  was an explicit user-requested QoL feature,
                                  not strict-rules behavior — see section 4).
  ui/game/stock_card.gd               StockCard (PanelContainer): themed
                                  "certificate" card for a held chain's
                                  stock (name/count/price), colored by
                                  ThemeDef.chain_color(). Reused (smaller) for
                                  bank-remaining indicators in the chains
                                  table, and restyled with a +/- stepper for
                                  the buy-phase cards (built inline in
                                  game.gd's _build_buy_card(), not via the
                                  StockCard class itself).

  ui/menu/MainMenu.tscn              Pre-game menu scene; project.godot's
                                  run/main_scene. Bare Control + script, same
                                  build-everything-in-code style as Game.tscn.
  ui/menu/menu.gd                    Name field + Hotseat/Host/Join buttons +
                                  host-port/join-address/join-port fields
                                  (the same fields that used to live in
                                  game.gd's footer). On submit, stashes the
                                  choice into the NetConfig autoload (net/
                                  net_config.gd) and calls
                                  change_scene_to_file("res://ui/game/
                                  Game.tscn"); that scene's _ready() reads
                                  NetConfig.has_pending and auto-hosts/auto-
                                  joins/starts hotseat accordingly, then
                                  clears it.

  ui/board/*  (legacy, unused — see above)

================================================================================
3. CURRENT STATE & ACTION PLAN
================================================================================
100% COMPLETE (verified, working, tested):
  - Pure rules engine (core/): found/grow/merge incl. survivor-excludes-new-
    tile rule, tie detection, safe chains (size>=11) and dead-tile detection
    (would merge two safe chains), majority/minority bonuses incl. sole-
    holder and tie-split cases, stock disposal (sell/trade 2-for-1/keep),
    buy limits (3 shares/turn, bank/cash validation), end-game trigger +
    scoring. 77/77 headless test assertions pass.
  - Rules-document audit completed: engine cross-checked line-by-line against
    ACQUIRE RULES.txt. Two real gaps found and fixed (see section 4 for the
    "closest to 1A" ranking bug found along the way):
      1. Starting-draw setup phase (rules step 8) — now implemented in
         GameState.setup_new_game() via _deal_starting_tiles().
      2. "Founding with zero stock available" rare case — founder now gets
         cash instead of a share when the bank has none left to give.
  - Full interactive hotseat UI (ui/game/): drag-and-drop tile placement
    (coordinate-locked), founding popup (color-coded chain buttons), merger
    resolution incl. tie-survivor popup and per-shareholder dispose flow,
    buy-phase stock-card steppers with live cross-stepper 3-share cap, end-
    turn/declare-game-end buttons, game-over standings screen.
  - Theme-driven visuals throughout: board cell colors, stock cards, buy
    cards, chains table, price chart, founding/tie buttons all read from
    ThemeDef — none hardcode a chain name or color.
  - UX polish round: yellow playable-square highlighting (correctly excludes
    temporarily-blocked 8th-chain squares), red dead-tile styling with a
    per-tile Redraw button, bottom-left chains table (company / size-icon /
    price / bank-stock-indicator) sized to half-width with a static stock
    price chart card filling the other half, hand tiles sorted left-to-right
    by column-then-row for readability.
  - Window-layout fix: scroll containers on both columns + a footer pinned
    outside any scroll, so the Generate Mid-Game / Reset test buttons can
    never be clipped off-screen regardless of window size; runtime minimum
    window size enforced (1000x620); viewport stretch mode set to "disabled"
    (1:1 pixels, no scaling artifacts).
  - Test tooling: "Generate Mid-Game" button (+ matching headless check
    sim/check_midgame.gd), "Generate Near-Endgame" button (+ matching
    headless check sim/check_near_endgame.gd, for exercising the "Declare
    Game End" button and GAME_OVER summary screen), and "Reset (Empty)"
    button, all in the always-visible footer.
  - Main menu / NetConfig (first of the post-M4 "presentation polish" plan's
    three stages — see below): ui/menu/MainMenu.tscn + ui/menu/menu.gd is now
    project.godot's run/main_scene. It collects a player name and a mode
    (Hotseat / Host / Join, the latter two reusing the address/port fields
    that used to live in the game screen's footer) and stashes the choice in
    a new autoload singleton, net/net_config.gd (registered as "NetConfig" in
    project.godot's [autoload] — note: no class_name on that script, since
    Godot 4 treats a class_name matching an autoload's own name as a
    conflict, not an alias). ui/game/game.gd's _ready() reads
    NetConfig.has_pending and auto-hosts/auto-joins/starts hotseat
    accordingly, then clears it; if has_pending is false (e.g. headless
    scripts that instance Game.tscn directly, or running Game.tscn standalone
    during dev) it falls back to exactly the old default solo-hotseat
    behavior — zero behavior change for every existing headless suite. The
    game screen's footer now only shows a live status label + the host's
    "Start Networked Game" button (address/port entry fields were removed,
    since that's the menu's job now); the game-over screen gained a
    "Main Menu" button next to "New Game", calling
    get_tree().change_scene_to_file("res://ui/menu/MainMenu.tscn"). New
    headless check: sim/check_menu_flow.gd (18 assertions) covers the
    MainMenu controls existing, the hotseat/host paths correctly consuming
    and clearing NetConfig, the no-pending-intent fallback matching legacy
    behavior, and the game-over screen's two buttons.
  - Animation layer (second of the three "presentation polish" stages):
    ui/game/game.gd's GameSession.event_applied handler no longer reacts to
    events inline. _on_event_applied() now only pushes onto _pending_events
    and kicks _drain_event_queue() (a coroutine) if one isn't already
    running; that coroutine pops one event at a time, awaits
    _play_event_animation(event) (a no-op for any event type with no
    animation yet), then runs the unchanged old reaction logic — renamed
    _apply_event_ui() — before moving to the next queued event. This exists
    because GameSession can emit several events for one action (e.g. a
    merger's PENDING/STARTED/dispose/FINISHED burst, all emitted
    synchronously in net/session.gd's _process_action loop); a coroutine
    connected directly to the signal would let those animations overlap,
    since Godot's emit() doesn't block on an awaiting listener — queueing
    guarantees they never do. A new bool, _animating, is true for the
    duration of each event's animation; can_drag_tiles()/can_drop_tile() and
    every Button still showing in _action_box (via the new
    _lock_action_box()/_disable_buttons_recursive() helpers — the action box
    itself isn't rebuilt until the animation finishes) are locked while it's
    true, so a player can't act on stale, pre-event controls mid-animation.
    A new top_level Control, _fx_layer (built once in _build_layout(), last
    child so it renders above everything), hosts transient animated visuals
    spawned by _slide_visual() (a duplicate-styled label-on-panel tweened
    between two global positions via create_tween()). Three concrete
    animations ship now: _animate_tile_placed (TILE_PLACED: slides from the
    RackTile still shown pre-refresh to its BoardCell), _animate_stock_bought
    (STOCK_BOUGHT: slides from the buy-stepper card — tagged with
    set_meta("chain", ...) in _build_buy_card() so _find_buy_card_node() can
    locate it — into the stock sidebar), and _animate_merger_started
    (MERGER_STARTED: a brief modulate flash over every BoardCell the
    survivor chain now occupies — queried post-mutation since the host
    already applied the real merge before broadcasting, so this
    intentionally flashes the whole chain rather than tracking exactly which
    cells just changed hands). A bool, _animations_enabled (default true),
    gates _play_event_animation() entirely; every headless sim/check_*.gd
    script that drives session.send_action() against a real Game.tscn
    instance sets it false immediately after instantiating, keeping those
    suites fully synchronous and exactly as fast as before this stage. New
    headless check: sim/check_animations.gd (10 assertions, animations left
    ON) — unlike the other check_*.gd scripts it's a small _step state
    machine polled across several real _process() ticks (Tweens need real
    frames/time to resolve), confirming _animating flips true the instant an
    animated action is sent, drag/drop and action-box buttons are locked out
    meanwhile, and the model ends up exactly where the no-animation path
    already proves correct once each animation finishes.
  - Hand "picked up by the mouse" drag affordance: game.gd's begin_drag()
    (called from RackTile._get_drag_data(), i.e. the instant a drag starts)
    now calls _lift_rack_tile(coord), which hides that RackTile entirely
    (visible = false) so the HFlowContainer reflows, then plays a FLIP-style
    tween (_tween_rack_to(), snap-to-old-position + tween-to-new-position)
    on the hand's remaining direct children so they visibly slide over to
    close the gap instead of popping. end_drag() (called both from the
    success path in drop_tile(), and unconditionally from
    NOTIFICATION_DRAG_END) now distinguishes success from a missed/cancelled
    drop via a plain bool, _drag_drop_succeeded — set true only by
    drop_tile() right before its own end_drag() call — rather than trusting
    Viewport.gui_is_drag_successful()'s timing relative to that call. On a
    failed drop, end_drag() calls _return_lifted_tile(), which slides a
    ghost (reusing the existing _slide_visual() helper from the Stage-2
    animation layer) from wherever the mouse released back to the tile's
    remembered hand-slot position (_drag_origin_pos, captured in
    _lift_rack_tile() before hiding it), then re-shows the real RackTile and
    plays the same FLIP tween in reverse so the rest of the hand slides back
    open to make room for it. This is pure drag-gesture presentation,
    independent of _animations_enabled (which only gates the event-driven
    animations once an action's outcome is already known) — it always runs,
    even in headless check_*.gd scripts, but none of them exercise real
    drag-and-drop (they call session.send_action() directly), so this didn't
    require touching any existing suite. _tween_rack_to() operates on
    _rack_box's direct children (a bare RackTile for a live tile, or the
    whole VBoxContainer for a dead tile + its Redraw button) rather than
    reaching into the RackTile alone, so a dead tile's Redraw button slides
    as one unit with it instead of tearing away mid-tween.

IN PROGRESS:
  - "Presentation polish" plan (main menu -> animation layer -> retheme
    groundwork, in that user-chosen order; M4 Stages A-B and the end-game
    flow were already done before this plan started). All three stages are
    now DONE — see their full writeups above/below under 100% COMPLETE /
    COMPLETED MILESTONES. M4 has since progressed through Stage C as well
    (see "NEXT MILESTONES" below for live status). All seven headless suites
    green (run_tests.gd 77/77, check_midgame.gd 18/18, run_network_tests.gd
    convergence test, check_near_endgame.gd 20/20, check_menu_flow.gd 18/18,
    check_animations.gd 10/10, check_seat_gating.gd 23/23 — see section 4 for
    how to run all of them).

COMPLETED MILESTONES:
  1. Presentation polish, Stage 3 — retheme groundwork. ✅ COMPLETE.
     Extended theme/theme_def.gd with new themeable fields (term_corporation,
     term_stock, term_stockholder, term_money, term_merger, currency_symbol)
     and created theme/fantasy_theme.gd with medieval fantasy chain names and
     terminology. Routed ~25 hardcoded user-facing strings in ui/game/game.gd
     through _theme.* fields instead of literals: "Your Stock" → "Your [term]",
     "$%d" → "[currency]%d", "Merger" → "[term_merger]", etc. Commits: d2b8731.
     Verification: all 6 headless suites pass; default theme produces identical
     UI text as before (pure refactor); fantasy_theme.gd is now a drop-in
     medieval re-skin ready for testing.
  2. M5 — Theming pass. ✅ COMPLETE. fantasy_theme.gd as authored in Stage 3
     didn't actually load: it redeclared @export fields (color_background,
     color_empty, etc.) already present on its parent ThemeDef, which
     GDScript rejects outright ("member already exists in parent class").
     Fixed by setting those fields' values in _init() instead of redeclaring
     them, and diversified chain_colors away from mostly-gold into seven
     distinct hues (steel gray/amber gold/crimson red/deep blue/forest
     green/royal purple/dark teal). Added theme/fantasy_theme.tres (the
     loadable resource pointing at the fixed script) and a footer button in
     ui/game/game.gd ("Switch to Classic/Fantasy Theme") that swaps the
     active ThemeDef and rebuilds the UI at runtime — purely cosmetic, never
     touches GameState. The rebuild is deferred via call_deferred() rather
     than run inline in the button's own pressed handler, since freeing the
     button's node tree (which _build_layout() does on every rebuild) while
     still mid-emission of that same button's "pressed" signal crashes the
     engine — Godot's own error for this names the fix. Disabled while
     networked (_is_networked), since a rebuild would reset the network-
     status row's live text back to its just-built default and that text
     has no single source of truth to recompute from. Commit: 77fe9ec.
     Verification: all 6 headless suites pass (this is UI-only, no engine
     change); confirmed via --import that FantasyTheme registers with no
     compile errors.

NEXT MILESTONES (in order):
  1. M4 — Networked multiplayer. Staged per net/session.gd's host-
     authoritative design (full plan: a now-historical plan doc; this section
     is the live status).
       STAGE A — DONE: Action/Event plumbing + loopback transport + the
         ui/game/game.gd refactor.
           - net/action.gd, net/event.gd: Action/Event are plain Dictionaries
             ({type, player, payload}); these two files hold the type
             constants and make_*() builders.
           - net/network_transport.gd: abstract interface (host/join/
             send_action/broadcast_event/send_event_to, signals
             action_received/event_received/peer_joined/peer_left).
           - net/loopback_transport.gd + net/loopback_hub.gd: in-process,
             zero-socket transport — peer id is registration order on a
             shared LoopbackHub (host is always peer 0). This is what makes
             the whole layer testable headlessly with no real networking.
           - net/session.gd (GameSession): the host-authoritative router.
             Host always independently re-runs the same GameState calls
             ui/game/game.gd used to call directly (classify_placement,
             do_found, merger_begin, etc.) and broadcasts an Event built from
             the *actual* result; clients never re-derive an outcome — they
             replay the identical state.* calls from the event's payload
             (_apply_event_to_state). This is what guarantees host/client
             convergence by construction.
           - ui/game/game.gd: every former direct state.do_xxx() call site
             (drop_tile, _on_found_chosen, _on_survivor_chosen,
             _on_dispose_confirm, _commit_buy, _redraw_tile) now builds an
             Action and calls session.send_action(); a single dispatcher
             _on_event_applied() (driven by session.event_applied) does all
             the UI-state updates that used to live inline in those
             functions. _is_my_turn()/_is_my_seat() seat-gating helpers exist
             and disable input + show a "Waiting for ...'s move" label for
             non-active seats, but are inert by default: _ready() wires up a
             solo GameSession with hotseat_mode = true (one peer, every seat
             playable locally), which is what reproduces today's hotseat
             behaviour unchanged. _generate_midgame() dev harness still pokes
             GameState directly (session.state = state) — intentionally not
             routed through Actions, since it's test tooling, not real play.
           - sim/run_network_tests.gd: headless convergence test — two
             GameSessions (host seat 0 / client seat 1) share a LoopbackHub
             and auto-play a full game purely through session.send_action(),
             asserting host/client GameState deep-equality after every single
             action. This is the regression net for the Action/Event layer;
             run it alongside the other two suites (section 4).
       STAGE B — DONE: net/enet_transport.gd + a minimal lobby row in
         game.gd's footer.
           - net/enet_transport.gd extends Node (NOT NetworkTransport's
             RefCounted base — Godot's high-level RPC system requires a Node
             in the scene tree so a peer's RPC lands on the matching node at
             the same NodePath on every machine). GDScript has no enforced
             interfaces, so it just exposes the identical method/signal
             surface NetworkTransport documents; net/session.gd's `transport`
             field is deliberately untyped (duck-typed) to accept either.
             Host/server is always peer id 1 (Godot's high-level multiplayer
             convention); client ids are ENet-assigned, NOT sequential by
             join order, so (unlike LoopbackTransport) peer id can't double
             as a seat index here.
           - Seat assignment: Event.make_game_started() gained a third
             `peer_seats` field (transport peer id -> seat index), built by
             the host's lobby (seat 0 = host, joiners get 1, 2, ... in
             connect order) and broadcast as part of the one GAME_STARTED
             event. Each client's GameSession looks itself up in that map
             (_on_event_received_as_client) and sets local_player_index —
             no separate private "you are seat N" message needed.
           - game.gd's footer originally gained a lobby row with its own
             host-port/join-address/join-port fields and Host/Join buttons.
             That row's input fields were later removed (presentation-polish
             Stage 1, see 100% COMPLETE above): hosting/joining now starts
             from ui/menu/menu.gd before Game.tscn is even loaded, carried
             via the NetConfig autoload; game.gd's footer today keeps only
             the live status label and the host-only "Start Networked Game"
             button. Either path replaces the default solo `session` with a
             real EnetTransport-backed one (hotseat_mode = false) and nulls
             out `state` until the real GameState arrives via GAME_STARTED
             (can_drag_tiles/can_drop_tile/_refresh_all all guard against
             state == null — the board just freezes rather than crashing
             while a peer waits in the lobby). The dev-tooling row (Generate
             Mid-Game/Generate Near-Endgame/Reset) auto-hides once any
             remote peer has joined (_dev_tools_visible()), since those
             buttons poke GameState directly and would desync anyone else
             watching.
           - No new headless test for Stage B by design: real socket/RPC
             behavior can't be exercised without an actual transport, so
             verification here is the manual two-window LAN checklist
             (now-historical plan doc has the original script; section 4
             below keeps the live version).
       STAGE C — DONE: verify seat-gating (_is_my_turn/_is_my_seat) actually
         disables the right controls once Stage B's hotseat_mode is false;
         sim/check_seat_gating.gd (23 assertions, 0 failures). No real
         networking needed — this tests game.gd's UI-gating logic, not
         engine convergence, which run_network_tests.gd already covers.
           - Two Game.tscn instances (ctrl_a = local seat 0, ctrl_b = local
             seat 1), each deferred one process tick per the usual
             check_*.gd gotcha (section 4), share ONE hand-authored
             GameState object assigned directly to both controllers'
             `state`/`session.state` fields, exactly like the existing
             dev-harness pattern (`ctrl.state = state; ctrl.session.state =
             state` — see _generate_midgame()/_generate_near_endgame()).
             Both sessions get hotseat_mode = false and their own
             local_player_index (0 / 1).
           - PLACE_TILE/FOUND_CHAIN/BUY_STOCK are all gated by the exact same
             one-line _is_my_turn() check at _rebuild_action_area()'s top, so
             one shared scenario is mutated phase-to-phase
             (state.phase/state.current_player flipped directly) rather than
             rebuilt three times: with current_player = 0, ctrl_a sees real
             phase controls and can_drag_tiles() is true (PLACE_TILE only)
             while ctrl_b sees the "Waiting for ...'s move" label; flipping
             current_player = 1 reverses both, confirming the gate isn't
             hardcoded to seat 0.
           - RESOLVE_MERGER's dispose sub-mode is gated per-shareholder via
             _is_my_seat() on _disposal_queue[0].player, NOT per-current-
             player: with state.current_player deliberately left at 0 and a
             _disposal_queue entry owed to player 1, ctrl_a (seat 0) still
             gets the waiting label despite "owning" current_player, while
             ctrl_b (seat 1) gets the real dispose SpinBoxes.
             _merge_mode/_disposal_queue/_merge_survivor are controller-local
             fields (not on GameState), set independently on each controller.
           - can_drop_tile()'s contract is pinned explicitly: it has its own
             substantial guard (data-shape, phase/animating, coord-match,
             placement-legality) but no seat check of its own — called
             directly for an inactive seat's own legal tile, it returns true,
             proving the seat gate lives solely in can_drag_tiles() (via
             RackTile._get_drag_data(), which refuses to return drag data at
             all when can_drag_tiles() is false). A future refactor that adds
             a seat check inside can_drop_tile() must flip this assertion.
           - No real session.send_action() calls or extra event_applied
             wiring: every mutation is direct state/controller-field
             assignment + _refresh_all(), the same technique
             _generate_midgame()/_generate_near_endgame() already use test-
             side — driving real Actions would need a second
             LoopbackHub/LoopbackTransport pair connecting the two sessions
             for no payoff, since convergence is already
             run_network_tests.gd's job.
       STAGE D — NOT STARTED: ai/bot_player.gd, a simple rule-based bot that
         calls session.send_action() like any other seat, for solo seat-
         filling; sim/check_bot_game.gd.
       STAGE E — NOT STARTED: consolidate the new sim/ suites into the
         "always re-run" discipline (already done for run_network_tests.gd —
         see section 4 below).
     FUNCTIONAL-EXPERIENCE GAPS found while planning Stage C (none block
       Stage C itself, but matter before real networked play feels finished
       — listed here so they aren't lost; none are started):
       - Joiner's chosen name (typed into ui/menu/menu.gd, stashed in
         NetConfig) never reaches the host. _on_start_networked_game_pressed()
         always labels remote seats "Player 2", "Player 3", ... — the name
         a joining player typed is discarded. Needs a small payload (e.g. on
         EnetTransport connect, or folded into the join handshake) carrying
         the joiner's name to the host before host_start_game() builds
         `names`.
       - No mid-game disconnect recovery: _on_lobby_peer_left() only fires
         pre-game (lobby phase); there's no handler for a peer dropping
         DURING play. If it's that seat's turn when they drop, the game just
         hangs forever (no skip-turn, no bot takeover, no reconnect path).
         At minimum needs a visible "seat N disconnected" state; a real fix
         probably wants Stage D's bot able to take over an orphaned seat.
       - No client reconnect: if a client's connection drops and they relaunch
         and rejoin, they have no way to resync to the in-progress GameState
         (the host's GAME_STARTED broadcast only fires once, at the start).
       - Host has no lobby validation — "Start Networked Game" is clickable
         with zero peers joined (silently starts a 1-player game) and there's
         no max-player enforcement against board/tile-supply limits.
       - "Main Menu" button on the game-over screen doesn't visibly tear down
         the EnetTransport child node (added with a fixed name "EnetTransport"
         — see _host_networked_game()'s comment on why) before the scene
         change; worth confirming hosting/joining a second game in the same
         process session doesn't hit a duplicate-node or stale-RPC-binding
         issue. Untested today since every session so far has been single-
         game-then-quit.
  2. M6 — Steam packaging. GodotSteam integration (steam_transport.gd behind
     the same network_transport.gd interface), Steam App ID + steam_appid.txt
     for local testing, Windows + Linux export presets, Steam Deck input/
     resolution pass, steamcmd depot upload docs.

================================================================================
4. CRITICAL CONTEXT
================================================================================
RULES CORRECTNESS (load-bearing, do not regress):
  - Merger survivor/bonus sizes are computed from PRE-merger chain sizes —
    the tile that causes the merger is NEVER counted toward either chain's
    size when picking the survivor or computing bonuses. Enforced by
    test_merger_excludes_new_tile() in sim/run_tests.gd. This is the single
    easiest Acquire rule to get wrong; do not "simplify" merger code without
    re-running that test.
  - "Closest to 1A" for starting-tile turn order is ROW-MAJOR, not column-
    major: rank = row * BOARD_WIDTH + col. This was gotten wrong on the
    first implementation attempt (used col-major) and caught only because
    the rules text's own example ("9A is closer to 1A than 1B") was encoded
    as an explicit test assertion. If this logic is ever touched again,
    re-derive from that exact sentence in ACQUIRE RULES.txt, don't trust
    intuition.
  - Bonus rounding: ties round UP to the nearest $100 (StockMarket-adjacent
    helper _round_up_100() in game_state.gd). This is in the official rules
    text verbatim ("rounded up to the nearest hundred").
  - The price/bonus formulas were independently validated against the rules
    doc's own Tycoon-mode worked example (Imperial, size 5: price 700,
    primary/majority 7000, tertiary 3500) even though this project only
    implements Classic Mode (2 payout tiers: majority/minority, no
    tertiary) — Classic Mode's "minority" bonus is mathematically the same
    5x-price tier as Tycoon Mode's "tertiary" in that example, which is how
    the cross-check works. Tycoon Mode (3-tier bonus) is NOT implemented and
    not currently planned.

DELIBERATE RULE DEVIATION (user-requested, keep as is):
  - Strict rules say unplayable ("dead") tiles are only set aside at END of
    turn, and any newly-dead tile from that turn's draw must wait until next
    turn to be discarded. This project instead gives the player an immediate
    "Redraw" button next to any dead tile in their rack, usable any time
    during PLACE_TILE phase. This was an explicit user request (QoL during
    testing), not an oversight — do not "fix" this to match strict rules
    without asking first.

ENGINE GOTCHAS HIT AND FIXED (so they aren't rediscovered):
  - GDScript type inference: untyped Array literals used in typed contexts
    (e.g. `const DIRS := [Vector2i(...), ...]`) fail to compile under strict
    typing with "Cannot infer the type of variable" — must declare as
    `Array[Vector2i]` explicitly. Hit once in game_state.gd's DIRS constant;
    if a similarly "obviously typed" const array gets added, declare its
    element type explicitly up front.
  - BoardCell.setup() must NOT call refresh() during construction — cells
    are built (in game.gd's _build_layout) BEFORE a GameState exists; calling
    state.cell() at that point hits a null `_ctrl.state`. The controller's
    _refresh_all() (called once after setup_new_game()/_generate_midgame())
    is solely responsible for the first paint.
  - Headless SceneTree test scripts that instance a scene and immediately
    call a method on it must defer to at least one _process() tick before
    interacting with it if the scene's _ready() builds UI children — calling
    methods from _initialize() (same frame as add_child) runs before _ready,
    so children referenced by the controller are still null. See
    sim/check_midgame.gd's file-header comment.
  - Button.text does not support `&` mnemonics being escaped — a literal "&&"
    in a button label renders as a doubled "&&" on screen, not "&". Use a
    single "&" if you want a literal ampersand in a Godot Button's text.

PROJECT-LEVEL CONSTRAINTS:
  - Git repository, initialized mid-project (first commit covers rules
    engine, hotseat UI, and M4 Stages A-B). No remote configured as of this
    writing.
  - Windows machine; PowerShell is the shell tool used for all Godot CLI
    invocations (Bash tool also available, both work, PowerShell used in
    practice throughout this project's history).
  - Always re-run ALL SEVEN headless suites after any change to core/,
    net/, ui/game/game.gd, or ui/menu/menu.gd before declaring work done:
      godot --headless --path <proj> --import   (only needed if a new
        class_name was added/removed; harmless to always run)
      godot --headless --path <proj> --script sim/run_tests.gd
      godot --headless --path <proj> --script sim/check_midgame.gd
      godot --headless --path <proj> --script sim/run_network_tests.gd
      godot --headless --path <proj> --script sim/check_near_endgame.gd
      godot --headless --path <proj> --script sim/check_menu_flow.gd
      godot --headless --path <proj> --script sim/check_animations.gd
      godot --headless --path <proj> --script sim/check_seat_gating.gd
    Current expected output: "==== 77 passed, 0 failed ====",
    "==== 18 passed, 0 failed ====", a convergence-test pass count with
    0 failed, "==== 20 passed, 0 failed ====", "==== 18 passed,
    0 failed ====", "==== 10 passed, 0 failed ====", and "==== 23 passed,
    0 failed ====", all exit code 0.
    sim/check_animations.gd takes noticeably longer than the others (well
    under a second, but non-zero) since it's the one script that lets real
    Tweens run across real frames/time instead of disabling animations — see
    its file-header comment. (A handful of "ObjectDB leaked"/"resources
    still in use at exit" warnings on process exit are pre-existing noise
    from headless scripts loading scenes/resources without explicit
    cleanup — not a regression signal; only the failed-count and exit code
    matter.) sim/check_bot_game.gd will join this list once M4 Stage D lands.
